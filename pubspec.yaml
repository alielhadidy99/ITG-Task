name: itg_marvel_task
description: ITG Marvel task

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  crypto: ^3.0.1
  convert: ^3.0.1
  dio: ^4.0.1
  get_it: ^7.2.0
  provider: ^6.0.1
  flutter_spinkit: ^5.1.0
  easy_localization: ^3.0.0
  clippy_flutter: ^2.0.0-nullsafety.1
  cached_network_image: ^3.1.0
  webview_flutter: ^2.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/images/
    - assets/translations/